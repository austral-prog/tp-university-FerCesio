package com.university;

import java.util.List;
import java.util.Map;

public class Borrador {
    // for (Map.Entry<String, Qualifications<Examination, List<Integer>>> entry : subject.markList.entrySet()) {
    //                        String key = entry.getKey();
    //                        Qualifications<Examination, List<Integer>> value = entry.getValue();
    //                        List<Integer> finalList = value.getList();
    //                        String lines = String.join(",", subject.getName(), value.getName().getExamName(), key, String.valueOf(average(finalList)));
    //                        bw.write(lines);
    //                        bw.newLine();
    //                    }
    //                      if (!subject1.markList.containsKey(studentName)) {
    //                            subject1.markList.put(studentName, new Qualifications<>(exam1, new ArrayList<>()));
    //                        } else if (subject1.markList.get(studentName).getName().getExamName().equals(evaluationName)) {
    //                            (subject1.markList.get(studentName).getList()).add(grade);
    //                        }



   //* for (Subject subject : University.subjects) {
    //        for (Examination exam : subject.examList){
        //          for (Map.Entry<Student, List<Integer>> entry : exam.grades.entrySet()){
        //            Student key = entry.getKey();
        //          List<Integer> gradeList = entry.getValue();
        //          String lines = String.join(",",subject.getName(),exam.getExamName(),key.getName(),String.valueOf(average(gradeList)));
        //          bw.write(lines);
        //          bw.newLine();
        //      }
      //  }

    //for (Student student : University.students) {
    //    for (Subject subject : student.subList){
    //        for (Examination exam : subject.examList){
    //            for (Map.Entry<Student,List<Integer>> entry : exam.grades.entrySet()){
    //                Student key = entry.getKey();
    //                List<Integer> gradeList = entry.getValue();
    //                String lines = String.join(",",subject.getName(),exam.getExamName(),student.getName(),String.valueOf(average(gradeList)));
    //                bw.write(lines);
    //                bw.newLine();
    //            }
    //        }
    //    }
   // }

}


